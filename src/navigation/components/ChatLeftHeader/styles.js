import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
  },
  ownerNameTitle: {
    marginLeft: 8,
    fontWeight: "500",
    fontSize: 16,
    lineHeight: 24,
  },
});
export default styles;
